extends partial/layout
include mixins/tags

block title
  title= __('Tags') + ' · ' + config.title
  
block container
  .tagcloud
    div.total
      != list_categories({style: '', separator: '/'})

